import {
  SUN,
  CLOUD,
  PARTLY_CLOUDY,
  RAIN,
  STORM,
} from "src/global/helpers/consts";

export const clearlyWeather = [SUN];

export const nastyWeather = [CLOUD, PARTLY_CLOUDY, RAIN, STORM];

export const NASTY_BG = "rgb(114, 144, 185)";

export const CLEARLY_BG = "rgb(73, 140, 236)";
